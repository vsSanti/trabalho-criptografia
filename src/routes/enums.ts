import { DashboardOutlined } from '@ant-design/icons';

import { Dashboard } from 'pages';

const PATHS = {
  DASHBOARD: {
    path: '/dashboard',
    key: 'dashboard',
    name: 'Dashboard',
    Component: Dashboard,
    Icon: DashboardOutlined,
  },
};

export { PATHS };
